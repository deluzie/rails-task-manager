<div class="container">
  <h1>Task manager</h1>

  <ul>
    <% @tasks.each do |task| %>
      <li class="list-inline">
        <%= form_for(task) do |f| %>
        <%= f.check_box :completed %>
        <% end %>
        <%= link_to task.title, task_path(task) %>
        <%= link_to "🗑", task_path(task), method: :delete %>
      </li>
    <% end %>
  </ul>
  <p class="back_to_home"><%= link_to "Add new Task", "/tasks/new" %></p>
</div>
